spring.application.name=backend
# MySQL Configuration (uses environment variables for Render deployment)
# For local dev: set these in your IDE run config or use a .env file
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/attendflow_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:Irshan@456hus}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# Allow CORS for the React frontend
# On Render: set CORS_ALLOWED_ORIGINS env var to override this value
cors.allowed.origins=${CORS_ALLOWED_ORIGINS:https://vercel-frontend-snowy-nine.vercel.app,http://localhost:5173,http://localhost:3000}
# Server port (Render injects PORT automatically)
# Mail Configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:shaikirshan78@gmail.com}
spring.mail.password=${MAIL_PASSWORD:hfhuyzmjofnadazi}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.debug=false